---
import { solutions } from "@/data/utils";
import { Zap, BarChart3, ShieldCheck } from "@lucide/astro";

/**
 * Map “string” -> componente Astro real
 * (Esto es lo correcto con @lucide/astro)
 */
const iconMap = {
  Zap,
  BarChart3,
  ShieldCheck,
} as const;

type IconKey = keyof typeof iconMap;
---

<section class="py-14 px-4 sm:py-16 sm:px-6 lg:py-20">
  <div class="mx-auto max-w-6xl">
    <div
      class="reveal reveal--left grid gap-10 md:gap-12 lg:gap-16 md:grid-cols-2 items-start md:items-center"
      data-reveal=""
    >
      <div class="space-y-6 sm:space-y-7">
        <h2 class="text-2xl sm:text-3xl lg:text-4xl font-bold leading-tight">
          ¿Te sientes identificado?
        </h2>

        <ul class="space-y-3 sm:space-y-4">
          {[
            "Pierdes mucho tiempo en tareas manuales repetitivas.",
            "Tu competencia se ve más moderna y profesional que tú.",
            "No tienes control real de tus datos o clientes.",
            "Tu web actual es lenta y no te trae prospectos.",
          ].map((item) => (
            <li class="flex gap-3 text-slate-600 italic text-sm sm:text-base leading-relaxed">
              <span class="text-red-400 font-bold">✕</span> {item}
            </li>
          ))}
        </ul>

        <div class="p-5 sm:p-6 lg:p-7 bg-blue-600 rounded-2xl text-white">
          <p class="font-bold text-base sm:text-lg mb-0 italic leading-relaxed">
            Mi misión es eliminar estos obstáculos para que te enfoques en lo que mejor sabes hacer: dirigir tu negocio.
          </p>
        </div>
      </div>

      <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-1 gap-4 sm:gap-5">
        {solutions.map((sol) => {
          const IconComp = iconMap[sol.icon as IconKey];

          return (
            <div
              class="reveal reveal--right p-5 sm:p-6 border border-slate-100 rounded-2xl shadow-sm hover:shadow-md transition-shadow bg-slate-50"
              data-reveal
              style={sol.reveal}
            >
              <div class="flex items-start sm:items-center gap-3 sm:gap-4 mb-3">
                <div class="p-2 sm:p-2.5 bg-white rounded-lg shadow-sm shrink-0">
                  <IconComp class={sol.iconStyle} />
                </div>

                <span class="text-xs sm:text-sm font-bold text-blue-600 uppercase tracking-wider leading-snug">
                  {sol.benefit}
                </span>
              </div>

              <h3 class="text-lg sm:text-xl font-bold mb-2 leading-snug">
                {sol.title}
              </h3>

              <p class="text-slate-600 text-sm sm:text-base leading-relaxed">
                {sol.description}
              </p>
            </div>
          );
        })}
      </div>
    </div>
  </div>
</section>
